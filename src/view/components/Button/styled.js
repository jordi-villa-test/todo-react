import styled from 'styled-components';

const Button = styled.button`
  font-size: 14px;
  color: white;
  background: #0184e8;
  padding: 8px 16px;
  border-radius: 5px;
  cursor: pointer;

  &:hover {
    opacity: 0.8;
  }
`;

export default Button;
